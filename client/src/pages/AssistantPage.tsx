/** Route target so the header nav has no dead link. Real chat UI comes later. */
export default function AssistantPage() {
  return (
    <main className="page">
      <h1>Assistant</h1>
      <p>The travel assistant is not built yet.</p>
    </main>
  );
}
